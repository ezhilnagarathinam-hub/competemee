# Roadmap
- Continue tenant-scoped application data flows
- Add public Organization onboarding page with plan selection and registration entry from home (complete)
- Fix OCR and bulk parsing count/fragment errors without changing organization, admin, or student flows
